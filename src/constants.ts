/** Typo */
export const nbsp = "\u00A0";

/** Date */
export const DATE_FORMAT = "dd MMM, yyyy";

export const TIME_FORMAT = "H:mm";

export const FULL_DATE_FORMAT = `${DATE_FORMAT} ${TIME_FORMAT}`;

/** Deskpro */
export const DEFAULT_ERROR = "An error occurred"

export const AUTH_ERROR = "Go back to the admin settings form for the app and re-auth from there";

export const RATE_LIMIT_ERROR = "You have reached the maximum number of concurrent sessions permitted by your TeamViewer license";
